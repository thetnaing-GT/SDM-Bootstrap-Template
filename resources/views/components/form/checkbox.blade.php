<div class="form__check">
    <input type="checkbox" id="{{ $id }}" name="{{ $name }}" value="{{ $value }}"
        class="form__check-input" {{ $attributes }} />
    <label for="{{ $id }}" class="form__check-label">
        {{ $label }}
    </label>
</div>
